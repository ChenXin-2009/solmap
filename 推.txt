git status

	如果显示 Changes not staged for commit 或 Untracked files：

git add .
git commit -m "更新Next.js版本"

git checkout main
git pull origin main

git add .
git commit -m "Release 2.2.3: 更新Next.js版本"
git tag v2.2.3
git push origin main
git push origin v2.2.3
