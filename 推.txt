git status

	如果显示 Changes not staged for commit 或 Untracked files：

	git add .
	git commit -m "保存本地修改，准备新版本"

git checkout main
git pull origin main

git add .
git commit -m "Release 2.0.0: 更新了3D模式的基础功能"
git tag v2.0.0
git push origin main
git push origin v2.0.0
